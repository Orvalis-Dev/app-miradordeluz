[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm run build && pnpm preview --host"
restartPolicyType = "always"
restartPolicyMaxRetries = 5

[env]
# 🔐 Variables críticas para seguridad
FORCE_HTTPS = "true"
NODE_ENV = "production"
